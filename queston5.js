console.log(reverseVowels("haystack","needle"));
function reverseVowels(haystack,needle) {
   let haystack = "sadbutsad";
   let needle = "sad";
    let arr1 = haystack.split('');
     let arr2 = needle.split('');
     if(haystack.length >needle.length ){
        n = needle.length;
     } else {
        n = haystack.length;
     }
     let h = 0;
        for(let h = 0 ;j >= n;j++){
   for(let j = 0 ;j >= n;j++){
   if( arr[h] == arr [j]){
     return -1;
   } else {
    return 1 ;
   }
   }
};
